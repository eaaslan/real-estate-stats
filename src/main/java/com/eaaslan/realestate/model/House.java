package com.eaaslan.realestate.model;

import java.util.UUID;

public class House extends Residence{
    public House(double price, double squareMeter, int roomCount, int salonCount) {
        super(price, squareMeter, roomCount, salonCount);
    }
}
